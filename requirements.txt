# Stock Analysis & Backtesting Dashboard — pinned dependencies
# Python 3.11–3.14 supported (developed/tested on 3.14 with the versions below).
#
# NOTE on indicators / pandas-ta:
#   Indicators are computed in pure pandas (analysis/indicators.py). `pandas-ta`
#   is intentionally NOT listed here: its current PyPI release (0.4.x) hard-imports
#   `numba`, which has no Python 3.14 wheel, and the pure-Python classic release
#   (0.3.14b0) was removed from PyPI. Our implementations match its formulas.
#   To use the real package instead, run on Python 3.12 and `pip install pandas-ta`.

numpy==2.5.0
pandas==3.0.3
yfinance==1.4.1
backtesting==0.6.5
pywebview==6.2.1

# pywebview's Windows backend (Edge WebView2 via .NET). Not needed on macOS/Linux.
pythonnet==3.1.0 ; sys_platform == "win32"
